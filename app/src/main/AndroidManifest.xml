<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techkshetra.tpc1.h2ocustomer">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Splashscreentheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgotActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity
            android:name=".DashBoardActivity"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".FavouriteActivity" />
        <activity android:name=".FaqActivity" />
        <activity android:name=".AdvancecanActivity" />
        <activity android:name=".AddressActivity" />
        <activity android:name=".BuybigActivity" />
        <activity android:name=".BuyfiveActivity" />
        <activity android:name=".BuytwoActivity" />
        <activity android:name=".BuyoneActivity"></activity>
    </application>

</manifest>